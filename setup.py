from setuptools import setup, find_packages

setup(
    name="HyPy",
    version="0.0.1",
    description="A collection of graph, sorting, and search algorithms",
    python_requires=">=3.7",
    install_requires=[],
)
